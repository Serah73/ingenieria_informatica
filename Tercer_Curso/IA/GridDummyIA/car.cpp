#include "car.hpp"

/*
std::vector<int> car_t::north()
{
    std::vector<int> v;
    v.push_back(get_x() );
    v.push_back(get_y() + 1);
    
    return v;
}

std::vector<int> car_t::south()
{
    std::vector<int> v;
    v.push_back(get_x() );
    v.push_back(get_y() - 1);
    
    return v;
}

std::vector<int> car_t::west()
{
    std::vector<int> v;
    v.push_back(get_x() - 1);
    v.push_back(get_y() );
    
    return v;   
}

std::vector<int> car_t::east()
{
    std::vector<int> v;
    v.push_back(get_x() + 1);
    v.push_back(get_y() );
    
    return v;    
}*/

int car_t::move(int colour)
{
    //std::vector<int> window;    // ventana a la futura posicion del coche
    
}

